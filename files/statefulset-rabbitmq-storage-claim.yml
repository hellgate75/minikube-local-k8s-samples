apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: rabbitmq-storage-claim
  labels:
    volume: rabbitmq-storage-volume-claim
    type: storage-claim
    role: services
    app: flow-centric
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
  selector:
    matchLabels:
      volume: rabbitmq-storage
