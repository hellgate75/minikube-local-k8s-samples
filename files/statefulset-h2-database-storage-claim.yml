apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: h2-database-storage-claim
  labels:
    volume: h2-database-storage-volume-claim
    type: storage-claim
    role: services
    app: flow-centric
spec:
  accessModes:
  - ReadWriteMany
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
  selector:
    matchLabels:
      volume: h2-database-storage
