apiVersion: v1
kind: PersistentVolume
metadata:
  name: rabbitmq-storage
  labels:
    volume: rabbitmq-storage-volume
    type: storage
    role: services
    app: flow-centric
spec:
  storageClassName: manual
  accessModes:
  - ReadWriteMany
  capacity:
    storage: 1Gi
  persistentVolumeReclaimPolicy: Retain
  hostPath:
    path: /home/docker/data/rabbit-mq/
